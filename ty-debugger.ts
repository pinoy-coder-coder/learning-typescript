//TYPESCRIPT DEBUGGER
const courseName = "Typescrip course";

    debugger;

if (courseName) {
    
    const subtitle = "learning typescript and building project";

    printCourse(courseName);
}

function printCourse(name: string) {

    debugger;

    console.log("the course name: ", name.toLocaleUpperCase());
}